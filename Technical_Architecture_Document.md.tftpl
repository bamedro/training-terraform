# Technical Architecture Document (TAD) for ${platform.platform_code} on ${platform.environment}
Purpose of this document is to describe architecture details of the platform.
To remain as close as possible to the reality, this document lives in the same
repository as the infrastructure code, and is generated by Terraform as well.
